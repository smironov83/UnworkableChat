#include <stdlib.h>
#include "CommandModule.h"

auto main() -> int
{
	system("chcp 1251");
	system("cls");
	
	CommandModule* unworkableChat = nullptr;
	while (!(unworkableChat->LoginMenu() == 3))
	{

	}
	return 0;
}
